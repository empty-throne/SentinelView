import React from 'react'
import ReactDOM from 'react-dom/client'
import SentinelView from '../SentinelView.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SentinelView />
  </React.StrictMode>,
)
